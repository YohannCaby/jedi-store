server:
  port: 8080

spring:
  application:
    name: jedi-store-orchestrateur
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8888/realms/coruscant
      client:
        registration:
          mcp-server:
            provider: keycloak
            client-id: mcp-auth
            client-secret: A6UQC8t7Oap0tedgqNviWZvdVo3h1loQ
            authorization-grant-type: client_credentials
            scope: openid
        provider:
          keycloak:
            token-uri: http://localhost:8888/realms/coruscant/protocol/openid-connect/token
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: ministral-3:3b
    mcp:
      client:
        type: SYNC
        toolcallback:
          enabled: true
        streamable-http:
          connections:
            mcp-server:
              url: http://localhost:8081
              endpoint: /mcp
        initialized: false
logging:
  level:
    root: info