server:
  port: 8081

spring:
  application:
    name: jedi-store-mcp-server
  ai:
    mcp:
      server:
        protocol: streamable
        name: jedi-store-mcp-server
        version: 1.0.0
        type: SYNC
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8888/realms/coruscant
logging:
  level:
    org:
      springframework:
        ai:
          mcp: DEBUG
